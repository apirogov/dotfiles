-- allows ifdef constructions only for interpreter with CPP language pragma
:set -DGHCI

import Data.Function (on)
import Data.Ratio

-- :set -XOverloadedStrings
-- :set prompt "\ESC[1;37m%s\n\ESC[1;34mλ> \ESC[m"
:set prompt "λ> "
